/**
 * The shape of the user interface
 *
 * @export
 * @interface User
 */
export interface User {
    id: string;
    firstName: string;
    lastName: string;
    dateOfBirth: Date;
    address: any;
}